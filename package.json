{
  "name": "midifs",
  "version": "0.0.1",
  "description": "Read and write MIDI files via Node.js's fs (file system) module",
  "author": "Adam Murray",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "babel --out-dir lib src",
    "test": "mocha --compilers js:babel-core/register test/**/*.test.js"
  },
  "keywords": [
    "midi",
    "music"
  ]
}
